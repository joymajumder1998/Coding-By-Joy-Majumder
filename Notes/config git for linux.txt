git config --global user.email "joy@example.com"
git config --global user.name "Joy Majumder"
git config --global user.password "password" // Password or authentication token

-------------------------------------------------

git config --global credential.helper cache // Write this command if you don't want to type credentials every time
